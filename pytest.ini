[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -v --cov=crypt_tools
markers =
    smoke: minimal end-to-end checks that verify the CLI starts and performs a basic roundtrip
